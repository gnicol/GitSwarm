.form-group
  .col-sm-10
    - if git_fusion_import_enabled?
      .description
        Two-way mirroring! Project changes made here will be reflected in the Helix Versioning Engine, and changes
        made via Helix clients will be reflected in GitSwarm.

        .split
          .left
            .control-group.form-inline
              Git Fusion Server:
              - servers = git_fusion_servers
              = select_tag(:git_fusion_entry, servers, class: 'form-control')
            .js-toggle-content.hide
              .git-fusion-import-data
                .radio
                  = label_tag do
                    = radio_button_tag(:git_fusion_auto_create, true, checked: true)
                    .option-title
                      Create a new convention-based repo in Git Fusion
                    .option-descr
                      Create a Git Fusion repo that will be mirrored in Perforce under
                      %span.auto-create-path //gitswarm/projects/namespace/project-path/...
                .radio
                  = label_tag do
                    = radio_button_tag(:git_fusion_auto_create, false)
                    .option-title
                      Mirror an existing Git Fusion repository
                    .option-descr
                      - repos = git_fusion_repos
                      - if repos.empty?
                        .repo-warning
                          Although Git Fusion is configured, there are no repos available for import.
                      - else
                        .repo-select.form-inline
                          Repo:
                          = select_tag(:git_fusion_repo_name, '', class: 'select2 span2', disabled: 'true')
                %button.btn.btn-sm.btn-cancel.mirror-cancel{:type => "button"} Cancel Mirroring

          .right
            To point GitSwarm at a different Git Fusion instance, have an admin
            = link_to 'see these directions.', help_page_path('workflow', 'importing', 'import_from_gitfusion'), target: '_blank'
    - else
      .description
        This GitSwarm instance is not pointing to any Git Fusion servers. To import a repo from an existing Git Fusion
        instance, have an admin
        = link_to 'see these directions.', help_page_path('workflow', 'importing', 'import_from_gitfusion'), target: '_blank'
