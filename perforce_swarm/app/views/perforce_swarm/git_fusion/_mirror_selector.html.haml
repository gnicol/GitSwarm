- if !@errors.empty?
  .mirroring-errors
    %h4 Configuration Error:
    = @errors.first
    Please see
    = link_to ' this document ', help_page_path('workflow', 'importing', 'import_from_gitfusion') + '#known-issues'
    for help.
- else
  .mirroring-selection
    %p
      Your project's branches will be mirrored to:
      %span.depot-path
        = @path_template
    .form-actions
      = link_to '#', class: 'btn btn-create' do
        Launch Mirroring
