.git-fusion-import-data
  - repos = git_fusion_repos(@fusion_server)
  - if repos.empty?
    .repo-warning
      Although Git Fusion is configured, there are no repos available for import.
  - else
    .repo-select.form-inline.control-group
      Repo:
      = select_tag(:git_fusion_repo_name, repos, class: 'select2 span2', disabled: 'true')
