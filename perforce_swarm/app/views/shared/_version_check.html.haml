- status = 'critical'
%div{:class => ['version-check-status', 'alert', 'hidden-xs', "#{status}"]}
  - if status == VersionCheck::VERSION_CURRENT
    You are running the latest version of GitSwarm.
  - else
    This Installation of GitSwarm is out of date. There is an update available.
  .pull-right
    - if check_enabled.nil?
      Allow GitSwarm to keep checking for updates?
      = link_to 'Yes', '#', class: 'alert-link'
      |
      = link_to 'No', '#', class: 'no-version-check-setting alert-link'
    - else
      - unless more_info.nil? || more_info.empty?
        = link_to 'more info', more_info
        |
      = link_to "Don't show again for this version", '#', class: 'alert-link'
