- status = VersionCheck::outdated? ? 'needs-update' : 'current'
- status = VersionCheck::critical ? 'critical' : status
%div{:class => ['version-check-status', 'alert', 'hidden-xs', "#{status}"]}
  - if !VersionCheck::outdated?
    You are running the latest version of GitSwarm (#{PerforceSwarm::VERSION}).
  - else
    This Installation of GitSwarm is out of date. You are running #{PerforceSwarm::VERSION}, and the latest is #{VersionCheck::latest}.
  .pull-right.version-check-details
    - if current_application_settings.version_check_enabled.nil?
      Allow GitSwarm to keep checking for updates?
      = link_to 'Yes', application_settings_path(application_setting: {version_check_enable: true}), remote: true, method: :put, class: 'alert-link'
      |
      = link_to 'No', application_settings_path(application_setting: {version_check_enable: false}), remote: true, method: :put, class: 'alert-link'
    - else
      - if VersionCheck::more_info
        = link_to 'more info', VersionCheck::more_info, target: '_blank'
        |
      = link_to "Don't show again for this version", application_settings_path(application_setting: {last_version_ignored: VersionCheck::latest}), remote: true, method: :put, class: 'alert-link'
    = link_to 'X', '#', class: 'dismiss-version-check'
