# Configuration properties for validation tests

# Browser to run, eg: (phantomjs, firefox, chrome)
# Note a new version of the browser is required on the system.
#  * PhantomJS needs to be >= 2.0 in order to avoid syntax parse errors
#  * Chromedriver needs to be installed in order to run chrome
browser : phantomjs

gitswarm_url : http://CHANGE_ME/
gitswarm_username : root
gitswarm_password : 5iveL!fe

p4_port : ssl:CHANGE_ME:1666
p4_user : root
p4_password : 5iveL!fe
p4_gitswarm_depot_root : //gitswarm/projects/

git_binary : git

# Logging level.  DEBUG, INFO
log_level : INFO

# For enforce permissions tests
# secure_git_fusion            : secure-local
# secure_git_fusion_depot_root : //depot/

# run_id                       : value  # define a permanent non-unique run id.  Do not use in automation.
# skip_setup                   : true   # for test development. Do not use in automation.
# skip_teardown                : true   # for test development. Do not use in automation.
